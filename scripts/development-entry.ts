/** Bare package alias used only by the source-checkout launch channel. */
export const DEVELOPMENT_ENTRY_NAME = '@dsh-tool-retry-dev/plugin'

/** Stable public npm identity used by the official profile bundle and presets. */
export const OFFICIAL_PACKAGE_NAME = '@canglongcl/dsh-tool-retry'
